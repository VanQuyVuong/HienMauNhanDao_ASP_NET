<Page x:Class="DesktopApp_HienMauNhanDao_DN.Views.Admin.CapGiayChungNhanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1100"
      Title="Cấp Giấy Chứng Nhận Hiến Máu">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TOP BANNER -->
            <Border Grid.Row="0" Background="#991b1b" CornerRadius="18" Padding="24,20" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="📜 QUẢN LÝ &amp; CẤP GIẤY CHỨNG NHẬN HIẾN MÁU" Foreground="White" FontSize="20" FontWeight="Black"/>
                        <TextBlock Text="Duyệt và phát hành Giấy chứng nhận hiến máu nhân đạo điện tử cho Tình nguyện viên toàn thành phố" Foreground="#fecdd3" FontSize="13" Margin="0,6,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="btnRefresh" Content="🔄 LÀM MỚI" Height="40" Padding="16,0" Background="#7f1d1d" Foreground="White" FontWeight="Bold" FontSize="12" Cursor="Hand" Click="btnRefresh_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- FILTER BAR -->
            <Border Grid.Row="1" Background="White" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="14" Padding="16,12" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtSearch" Grid.Column="0" Height="40" Padding="14,0" VerticalContentAlignment="Center"
                             Background="#f8fafc" BorderBrush="#cbd5e1" BorderThickness="1" FontSize="13"
                             TextChanged="txtSearch_TextChanged" Margin="0,0,12,0"/>

                    <ComboBox x:Name="cbFilterStatus" Grid.Column="1" Height="40" FontSize="13" FontWeight="Bold" SelectionChanged="cbFilterStatus_SelectionChanged">
                        <ComboBoxItem Content="Tất cả trạng thái" IsSelected="True" Tag="ALL"/>
                        <ComboBoxItem Content="Đã cấp chứng nhận" Tag="DA_CAP"/>
                        <ComboBoxItem Content="Chưa cấp chứng nhận" Tag="CHUA_CAP"/>
                    </ComboBox>
                </Grid>
            </Border>

            <!-- DATAGRID + PAGINATION -->
            <Border Grid.Row="2" Background="White" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="16" ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="dgCertificates" Grid.Row="0" Margin="-1" AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                              RowHeight="54" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#f1f5f9">
                        <DataGrid.Columns>
                            <!-- CERTIFICATE CODE -->
                            <DataGridTemplateColumn Header="Mã Giấy / Mã Túi" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="#fff7ed" BorderBrush="#fed7aa" BorderThickness="1" Padding="10,4" CornerRadius="8" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding MaChungNhan}" Foreground="#c2410c" FontWeight="Black" FontSize="12" FontFamily="Consolas"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Họ và Tên TNV" Binding="{Binding TenTnv}" Width="180"/>
                            <DataGridTextColumn Header="Số CCCD" Binding="{Binding Cccd}" Width="130"/>
                            
                            <DataGridTemplateColumn Header="Nhóm Máu / Thể Tích" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Background="#ea580c" Padding="8,2" CornerRadius="6" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding NhomMau}" Foreground="White" FontWeight="Bold" FontSize="11"/>
                                            </Border>
                                            <TextBlock Text="{Binding TheTich, StringFormat={}{0} ml}" FontWeight="Bold" FontSize="12" Foreground="#475569" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Ngày Hiến Máu" Binding="{Binding NgayHien}" Width="140"/>
                            <DataGridTextColumn Header="Chiến Dịch Tổ Chức" Binding="{Binding TenChienDich}" Width="*"/>

                            <!-- STATUS -->
                            <DataGridTemplateColumn Header="Trạng Thái" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusBg}" Padding="10,4" CornerRadius="10" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusFg}" FontWeight="Black" FontSize="11"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- ACTIONS -->
                            <DataGridTemplateColumn Header="Thao Tác" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="📜 CẤP CHỨNG NHẬN" Background="#f8fafc" Foreground="#dc2626" BorderBrush="#fca5a5" BorderThickness="1" Padding="10,4" FontSize="11" FontWeight="Bold" Cursor="Hand" Click="btnIssueCert_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- PAGINATION -->
                    <Border Grid.Row="1" Background="#f8fafc" BorderBrush="#e2e8f0" BorderThickness="0,1,0,0" Padding="14,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Hiển thị: " Foreground="#64748b" FontSize="11" FontWeight="Medium" VerticalAlignment="Center"/>
                                <ComboBox x:Name="cbPageSize" Width="90" Height="28" FontSize="11" FontWeight="Bold" SelectionChanged="cbPageSize_SelectionChanged" Margin="4,0,12,0">
                                    <ComboBoxItem Content="5 dòng" IsSelected="True" Tag="5"/>
                                    <ComboBoxItem Content="10 dòng" Tag="10"/>
                                    <ComboBoxItem Content="20 dòng" Tag="20"/>
                                </ComboBox>
                                <TextBlock x:Name="txtPaginationInfo" Text="Đang tải..." Foreground="#0f172a" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="btnPrevPage" Content="◀ Trang trước" Height="28" Padding="10,0" Background="White" Foreground="#dc2626" BorderBrush="#fca5a5" BorderThickness="1" FontSize="11" FontWeight="Bold" Cursor="Hand" Click="btnPrevPage_Click" Margin="0,0,6,0"/>
                                <Border Background="#dc2626" CornerRadius="4" Padding="10,4" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <TextBlock x:Name="txtCurrentPage" Text="Trang 1 / 1" Foreground="White" FontSize="11" FontWeight="Black"/>
                                </Border>
                                <Button x:Name="btnNextPage" Content="Trang sau ▶" Height="28" Padding="10,0" Background="White" Foreground="#dc2626" BorderBrush="#fca5a5" BorderThickness="1" FontSize="11" FontWeight="Bold" Cursor="Hand" Click="btnNextPage_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
