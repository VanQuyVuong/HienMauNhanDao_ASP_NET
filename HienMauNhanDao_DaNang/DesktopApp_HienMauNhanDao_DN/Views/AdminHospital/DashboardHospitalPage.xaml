<Page x:Class="DesktopApp_HienMauNhanDao_DN.Views.AdminHospital.DashboardHospitalPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="1150"
      Title="Bảng Điều Khiển Admin Bệnh Viện">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BANNER -->
        <Border Grid.Row="0" Background="#0f766e" CornerRadius="16" Padding="20,16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="📊 BẢNG ĐIỀU KHIỂN &amp; TỔNG QUAN BỆNH VIỆN" Foreground="White" FontSize="18" FontWeight="Black"/>
                    <TextBlock x:Name="lblBannerHospital" Text="Theo dõi nhân sự y tế, tồn kho máu và hoạt động hiến máu tại Bệnh viện C Đà Nẵng" Foreground="#ccfbf1" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>

                <Button x:Name="btnRefresh" Grid.Column="1" Content="🔄 LÀM MỚI" Height="38" Padding="14,0" Background="#0d9488" Foreground="White" FontWeight="Bold" FontSize="11" Cursor="Hand" Click="btnRefresh_Click"/>
            </Grid>
        </Border>

        <!-- KPI CARDS -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="12" Padding="16,14">
                <StackPanel>
                    <TextBlock Text="CÁN BỘ &amp; Y BÁC SĨ BỆNH VIỆN" Foreground="#64748b" FontSize="10" FontWeight="Bold"/>
                    <TextBlock x:Name="txtKpiStaffCount" Text="0" Foreground="#0f766e" FontSize="24" FontWeight="Black" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#fca5a5" BorderThickness="1" CornerRadius="12" Padding="16,14">
                <StackPanel>
                    <TextBlock Text="NHÓM MÁU BÁO ĐỘNG KHO (&lt;10)" Foreground="#b91c1c" FontSize="10" FontWeight="Bold"/>
                    <TextBlock x:Name="txtKpiAlertStock" Text="0" Foreground="#dc2626" FontSize="24" FontWeight="Black" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="White" BorderBrush="#93c5fd" BorderThickness="1" CornerRadius="12" Padding="16,14">
                <StackPanel>
                    <TextBlock Text="TỔNG TÚI MÁU HIỆN CÓ" Foreground="#1d4ed8" FontSize="10" FontWeight="Bold"/>
                    <TextBlock x:Name="txtKpiTotalUnits" Text="0" Foreground="#2563eb" FontSize="24" FontWeight="Black" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- SUMMARY CONTENT BOXES -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- STOCK SUMMARY TABLE -->
            <Border Grid.Column="0" Background="White" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="14" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="🩸 TÌNH TRẠNG KHO MÁU THEO NHÓM MÁU" Foreground="#0f172a" FontSize="14" FontWeight="Black" Margin="0,0,0,12"/>

                    <DataGrid x:Name="dgStockSummary" Grid.Row="1" AutoGenerateColumns="False" 
                              CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                              RowHeight="42" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#f1f5f9"
                              VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nhóm Máu" Binding="{Binding NhomMauLabel}" Width="160"/>
                            <DataGridTextColumn Header="Số Lượng Tồn (Túi)" Binding="{Binding SoLuongTon}" Width="160"/>
                            <DataGridTextColumn Header="Ngưỡng An Toàn" Binding="{Binding NguongAnToanDisplay}" Width="140"/>
                            <DataGridTemplateColumn Header="Cảnh Báo" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding AlertBg}" Padding="8,4" CornerRadius="6" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding AlertText}" Foreground="{Binding AlertFg}" FontWeight="Bold" FontSize="11"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- QUICK ANNOUNCEMENT & STAFF SHORTCUTS -->
            <Border Grid.Column="2" Background="White" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="14" Padding="16">
                <StackPanel>
                    <TextBlock Text="📣 THÔNG BÁO BỆNH VIỆN MOIN " Foreground="#0f172a" FontSize="14" FontWeight="Black" Margin="0,0,0,12"/>

                    <Border Background="#f0fdf4" BorderBrush="#bbf7d0" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="✅ Chuẩn bị ngày hội hiến máu" Foreground="#166534" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Text="Bệnh viện C phối hợp tổ chức điểm tiếp nhận máu cố định." Foreground="#15803d" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#fff7ed" BorderBrush="#fed7aa" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock Text="⚠️ Cảnh báo máu hiếm O-" Foreground="#c2410c" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Text="Số lượng tồn kho nhóm máu O- đang giảm dưới ngưỡng 10." Foreground="#ea580c" FontSize="11" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Button x:Name="btnSendNotification" Content="✉️ GỬI THÔNG BÁO BỆNH VIỆN" Height="40" Background="#0f766e" Foreground="White" FontWeight="Bold" FontSize="12" Cursor="Hand" Click="btnSendNotification_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
