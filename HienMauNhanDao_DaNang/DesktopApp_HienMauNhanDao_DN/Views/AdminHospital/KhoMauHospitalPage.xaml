<Page x:Class="DesktopApp_HienMauNhanDao_DN.Views.AdminHospital.KhoMauHospitalPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="1150"
      Title="Quản Lý Kho Máu Bệnh Viện">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BANNER -->
        <Border Grid.Row="0" Background="#0f766e" CornerRadius="16" Padding="20,16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="🩸 QUẢN LÝ TỒN KHO MÁU BỆNH VIỆN" Foreground="White" FontSize="18" FontWeight="Black"/>
                    <TextBlock Text="Theo dõi số lượng lưu trữ thực tế của 8 nhóm máu và hệ thống cảnh báo ngưỡng an toàn (10 đơn vị)" Foreground="#ccfbf1" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>

                <Button x:Name="btnRefresh" Grid.Column="1" Content="🔄 LÀM MỚI" Height="38" Padding="14,0" Background="#0d9488" Foreground="White" FontWeight="Bold" FontSize="11" Cursor="Hand" Click="btnRefresh_Click"/>
            </Grid>
        </Border>

        <!-- SAFETY THRESHOLD ALERT BANNER -->
        <Border Grid.Row="1" Background="#fff7ed" BorderBrush="#fed7aa" BorderThickness="1" CornerRadius="12" Padding="14,10" Margin="0,0,0,14">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚠️ Quy định ngưỡng an toàn:" Foreground="#c2410c" FontWeight="Black" FontSize="12" Margin="0,0,6,0"/>
                    <TextBlock Text="Mỗi nhóm máu trong kho bệnh viện phải duy trì tối thiểu 10 túi máu. Các nhóm máu ít hơn 10 túi sẽ xuất hiện nhãn cảnh báo đỏ." Foreground="#ea580c" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DATAGRID + PAGINATION TOOLBAR -->
        <Border Grid.Row="2" Background="White" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="14" ClipToBounds="True" VerticalAlignment="Stretch">
            <Grid VerticalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="dgHospitalStock" Grid.Row="0" Margin="0" AutoGenerateColumns="False" 
                          CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                          RowHeight="46" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#f1f5f9"
                          VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <DataGrid.Columns>
                        <!-- BLOOD GROUP BADGE -->
                        <DataGridTemplateColumn Header="Nhóm Máu" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#ea580c" Padding="12,4" CornerRadius="8" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding NhomMauLabel}" Foreground="White" FontWeight="Black" FontSize="13"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Số Lượng Tồn Kho Thực Tế" Binding="{Binding SoLuongTonDisplay}" Width="240"/>
                        <DataGridTextColumn Header="Ngưỡng An Toàn Quy Định" Binding="{Binding NguongAnToanDisplay}" Width="220"/>

                        <!-- ALERT BADGE -->
                        <DataGridTemplateColumn Header="Trạng Thái Kho Máu" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding AlertBg}" Padding="10,4" CornerRadius="6" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding AlertText}" Foreground="{Binding AlertFg}" FontWeight="Black" FontSize="11"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- FOOTER STATS -->
                <Border Grid.Row="1" Background="#f8fafc" BorderBrush="#e2e8f0" BorderThickness="0,1,0,0" Padding="14,10">
                    <Grid>
                        <TextBlock x:Name="txtStockSummaryTotal" Text="Tổng cộng: 8 nhóm máu | 0 túi máu trong kho" Foreground="#0f766e" FontSize="12" FontWeight="Black"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>
