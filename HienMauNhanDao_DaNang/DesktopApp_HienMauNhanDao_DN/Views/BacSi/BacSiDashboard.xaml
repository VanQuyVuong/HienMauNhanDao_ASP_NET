<Window x:Class="DesktopApp_HienMauNhanDao_DN.Views.BacSi.BacSiDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bảng Điều Khiển Bác Sĩ Khám Lâm Sàng" Height="768" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <!-- Style cho Menu Button -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#cbd5e1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="18,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="Active"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#dc2626"/>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="Inactive"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#1e293b"/>
                                <Setter Property="Foreground" Value="#f8fafc"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#0f172a" BorderBrush="#1e293b" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header Brand Card -->
                <Border Grid.Row="0" Margin="15,20,15,25" CornerRadius="16" Padding="15,20">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#dc2626" Offset="0.0"/>
                            <GradientStop Color="#991b1b" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel>
                        <Border Width="50" Height="50" Background="#33ffffff" CornerRadius="25" Margin="0,0,0,12" HorizontalAlignment="Center">
                            <TextBlock Text="🩺" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="HIẾN MÁU NHÂN ĐẠO" Foreground="#ffe4e6" FontSize="11" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Text="BÁC SĨ KHÁM LÂM SÀNG" Foreground="White" FontSize="14" FontWeight="Black" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Menu Items -->
                <StackPanel Grid.Row="1" Margin="15,0">
                    <Grid Margin="0,4">
                        <Button x:Name="btnDanhSachChoKham" Content="📋  Danh Sách Chờ Khám" Style="{StaticResource MenuButtonStyle}" Click="btnDanhSachChoKham_Click"/>
                        <Border x:Name="badgeBorder" Background="#dc2626" CornerRadius="10" Padding="8,2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" IsHitTestVisible="False" Visibility="Collapsed">
                            <TextBlock x:Name="txtBadgeCount" Text="0" Foreground="White" FontSize="11" FontWeight="Black"/>
                        </Border>
                    </Grid>
                    
                    <Button x:Name="btnKhamLamSang" Content="🩺  Form Khám &amp; Lịch Sử" Style="{StaticResource MenuButtonStyle}" Click="btnKhamLamSang_Click"/>
                </StackPanel>

                <!-- Footer (Logout) -->
                <Button x:Name="btnLogout" Grid.Row="2" Content="🚪 ĐĂNG XUẤT" Margin="15,20,15,25" Height="44"
                        Foreground="White" FontWeight="Bold" FontSize="13" Background="#b91c1c" BorderThickness="0" Cursor="Hand" Click="btnLogout_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#b91c1c" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#dc2626"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="#f8fafc">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Window>
