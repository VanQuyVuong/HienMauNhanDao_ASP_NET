<Window x:Class="DesktopApp_HienMauNhanDao_DN.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng Nhập Hệ Thống Y Tế" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#fdf8f9">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CỔNG THÔNG TIN Y TẾ" Grid.Row="0"
                   FontSize="22" FontWeight="Bold" Foreground="#e62e43"
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
        
        <TextBlock Text="Đăng nhập dành cho cán bộ" Grid.Row="1"
                   FontSize="14" Foreground="#666"
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBlock Text="Tên đăng nhập" Grid.Row="2" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox x:Name="txtUsername" Grid.Row="3" Height="40" FontSize="14" 
                 Padding="10,8" Margin="0,0,0,15" BorderBrush="#ccc" BorderThickness="1"/>

        <TextBlock Text="Mật khẩu" Grid.Row="4" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="5" Height="40" FontSize="14" 
                     Padding="10,8" Margin="0,0,0,30" BorderBrush="#ccc" BorderThickness="1"/>

        <Button x:Name="btnLogin" Content="ĐĂNG NHẬP" Grid.Row="6" Height="45" 
                FontSize="16" FontWeight="Bold" Foreground="White" Background="#e62e43"
                BorderThickness="0" Cursor="Hand" Click="btnLogin_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
